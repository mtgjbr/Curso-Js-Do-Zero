const nome= "luiz"; // string
const nu1 = 10; // number
let nomeAluno; // undefined valor não definido, não aponta pra nenhum lugar na memoria
const sobrenomeAluno = null; // null valor nulo, ausência de valor, nao aponta pra nenhum lugar na memoria, uso para esocolha defaut, para zerar o valor de uma variavel
const aprovado = true; // boolean, duas possibilidades: true ou false (lógico)
console.log(typeof sobrenomeAluno, sobrenomeAluno);